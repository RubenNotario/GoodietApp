<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.goodiet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Goodiet">
        <activity android:name=".RecuperarContrActivity"></activity>
        <activity android:name=".ConfigurationActivity"></activity>
        <activity android:name=".FavoriteRecipesActivity"></activity>
        <activity android:name=".HomeActivity"></activity>
        <activity android:name=".LoginMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NameChangeActivity"></activity>
        <activity android:name=".ProfileActivity"></activity>
        <activity android:name=".DetalleRecetaActivity" />
        <activity android:name=".DespensaActivity"></activity>
        <activity android:name=".ListaDeCompraActivity"></activity>
        <activity android:name=".ListaRecetasActivity" ></activity>
        <activity android:name=".RegistroActivity"></activity>
    </application>

</manifest>